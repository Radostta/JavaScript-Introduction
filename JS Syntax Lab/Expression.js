function solution() {
    let innerExpression = (30 + 25) * 1/3 * (35 - 14 -12);

//let expression = Math.pow(innerExpression, 2);
    let expression = innerExpression ** 2;

    console.log(expression);
}